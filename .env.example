# =============================================================================
# AI Networking Lab Tutor - Environment Configuration
# =============================================================================
# This project uses NVIDIA NIMs for LLM inference and embeddings.
# For the AWS/NVIDIA hackathon, NIMs MUST be deployed on AWS EKS (self-hosted).
#
# Two deployment modes are supported:
# 1. SELF-HOSTED (REQUIRED for hackathon): NIMs running on AWS EKS with K8s
# 2. hosted (development only): NVIDIA's free hosted API for quick testing
# =============================================================================

# ========================================
# Simulator Connection (Required)
# ========================================
# NetGSim simulator is hosted as an external service on Railway
SIMULATOR_BASE_URL=https://netgenius-production.up.railway.app
SIMULATOR_TOKEN=TEST_TOKEN

# ========================================
# NVIDIA NIM Deployment Mode
# ========================================
# IMPORTANT: For hackathon submission, this MUST be set to "self-hosted"
# to use AWS EKS-deployed NIMs (required for AWS/NVIDIA hackathon eligibility)
#
# Options:
#   self-hosted: AWS EKS with K8s (REQUIRED for hackathon prize eligibility)
#   hosted:      NVIDIA free API (development/testing only, NOT eligible for prizes)
#
NIM_MODE=self-hosted

# ========================================
# Self-Hosted NIM Endpoints (REQUIRED for hackathon)
# ========================================
# These endpoints point to your NVIDIA NIMs deployed on AWS EKS.
# After deploying with CDK and kubectl, get these URLs from:
#   kubectl get svc -n nim
#
# Example format (replace with your actual EKS load balancer endpoints):
SELF_HOSTED_LLM_URL=http://your-llm-load-balancer-url.elb.us-east-1.amazonaws.com:8000/v1
SELF_HOSTED_EMB_URL=http://your-embedding-load-balancer-url.elb.us-east-1.amazonaws.com:8000/v1

# ========================================
# NVIDIA NGC API Key (REQUIRED for self-hosted NIMs)
# ========================================
# NGC API key is required to pull NIM container images from NVIDIA NGC registry.
# Get your NGC API key at: https://ngc.nvidia.com/setup/api-key
NGC_API_KEY=your-ngc-api-key-here

# ========================================
# Development Mode Settings (Optional - NOT for hackathon)
# ========================================
# These settings are ONLY for development/testing without AWS infrastructure.
# DO NOT use for hackathon submission as it won't meet eligibility requirements.
#
# Uncomment to use NVIDIA's free hosted API (bypasses AWS EKS requirement):
# NIM_MODE=hosted
# NVIDIA_API_KEY=your-nvidia-api-key-here
#
# Hosted API endpoints (auto-configured, no need to change):
# NVIDIA_HOSTED_LLM_URL=https://integrate.api.nvidia.com/v1
# NVIDIA_HOSTED_EMB_URL=https://integrate.api.nvidia.com/v1
# NVIDIA_LLM_MODEL=nvidia/llama-3.1-nemotron-nano-8b-v1
# NVIDIA_EMB_MODEL=nvidia/nv-embedqa-e5-v5

# ========================================
# Application Settings
# ========================================
LOG_LEVEL=INFO
DEBUG=false
