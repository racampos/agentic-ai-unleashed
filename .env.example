# ========================================
# AWS Credentials (NOT REQUIRED)
# ========================================
# NOTE: AWS credentials are NOT needed in this .env file!
# CDK and AWS CLI automatically use credentials from ~/.aws/
# Configure AWS CLI with: aws configure
#
# These variables are only here for legacy/programmatic access if needed:
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_REGION=us-east-1
# AWS_DEFAULT_REGION=us-east-1

# ========================================
# NVIDIA API Key (Required for hosted mode)
# ========================================
# Get your free API key at: https://build.nvidia.com/
NVIDIA_API_KEY=your-nvidia-api-key-here

# ========================================
# NVIDIA NGC API Key (Optional - only for self-hosted NIMs)
# ========================================
# NGC_API_KEY=your-ngc-api-key-here

# ========================================
# Simulator Connection (Required)
# ========================================
# NetGSim simulator is hosted as an external service on Railway
SIMULATOR_BASE_URL=https://netgenius-production.up.railway.app
SIMULATOR_TOKEN=TEST_TOKEN

# ========================================
# NIM Deployment Mode (Required)
# ========================================
# Set to "hosted" to use NVIDIA's free hosted API (recommended for hackathon)
# Set to "self-hosted" to use your EKS-deployed NIMs (optional, requires AWS setup)
NIM_MODE=hosted

# ========================================
# Self-Hosted NIM Endpoints (Optional - only if NIM_MODE=self-hosted)
# ========================================
# SELF_HOSTED_EMB_URL=http://your-embedding-nim-endpoint:8000/v1
# SELF_HOSTED_LLM_URL=http://your-llm-nim-endpoint:8000/v1

# ========================================
# NVIDIA Hosted API Endpoints (Used when NIM_MODE=hosted)
# ========================================
NVIDIA_HOSTED_LLM_URL=https://integrate.api.nvidia.com/v1
NVIDIA_HOSTED_EMB_URL=https://integrate.api.nvidia.com/v1

# Model names for NVIDIA hosted API
NVIDIA_LLM_MODEL=nvidia/llama-3.1-nemotron-nano-8b-v1
NVIDIA_EMB_MODEL=nvidia/nv-embedqa-e5-v5

# ========================================
# Development Settings
# ========================================
LOG_LEVEL=INFO
DEBUG=false
